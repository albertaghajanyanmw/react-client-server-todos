// todo: add and using all variables from theme
export default (theme) => ({
  modal: {
    minWidth: 500,
    maxWidth: 500,
    minHeight: 300,
    maxHeight: 300,
    backgroundColor: 'white',
  },
  description: {
    margin: '20px 0 0 0'
  }
});
